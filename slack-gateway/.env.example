# Slack App Configuration
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SIGNING_SECRET=your-signing-secret

# Gateway Configuration
PORT=3022

# AI Maestro Configuration
# AIMAESTRO_API=http://127.0.0.1:23000
# DEFAULT_AGENT=my-agent
# HOST_ID=my-host

# Security: Operator Slack user IDs (comma-separated, full trust - no content wrapping)
# OPERATOR_SLACK_IDS=U0000000000

# Admin API token (REQUIRED - gateway refuses to start without it).
# Generate with: openssl rand -hex 32
ADMIN_TOKEN=your-admin-token

# Cache TTLs (milliseconds)
# CACHE_AGENT_TTL_MS=300000
# CACHE_HOSTS_TTL_MS=60000
# CACHE_SLACK_USER_TTL_MS=600000

# Polling
# POLL_INTERVAL_MS=2000
# POLL_TIMEOUT_MS=5000

# Development
# DEBUG=true
